doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1= nombrePagina  
            p Agrega tu correo electronico para reestablecer la contraseña

            form.caja-login(method="POST" action="/reestablecer")
                .campo  
                    label(for="email") Email 
                    input(type="email" name = "email" placeholder="Tu Email" required value= email)

                .campo.enviar
                    input.boton(type="submit" value = "Restablecer password")
                .campo.acciones
                    a(href="/iniciar-sesion") Ya lo recordé, volver a login